<h1>Tracks</h1>

{{#each tracks}}

<h2>{{this.name}}</h2>
<p>{{this.previewtype}}</p>
<audio controls
        src="{{this.previewtype}}">
            Your browser does not support the
            <code>audio</code> element.
</audio>

{{/each}}
